import React from "react";

class Button extends React.Component {
  constuctor(props) {
    super(props);
    this.state = {};
  }
  render() {
    return <button />;
  }
}

export default Button;
